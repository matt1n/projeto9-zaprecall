import { Main } from "./Main"

export default function App({logo, play, virar, zap, almoust, forgot}){
    return(
      <Main logo={logo} play={play} virar={virar} zap={zap} almoust={almoust} forgot={forgot}/>
    )
}
